interface:
  display_name: "Posture Generator"
  short_description: "Generate pose PNGs with bilingual prompt files"
  default_prompt: "Use $posture-generator to create a cutout transparent white-model pose PNG and same-path bilingual prompt Markdown file from this reference image."

policy:
  allow_implicit_invocation: true
