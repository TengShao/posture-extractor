interface:
  display_name: "姿态提取器"
  short_description: "Extract posture into transparent PNGs with bilingual prompt files"
  default_prompt: "Use $posture-extractor to extract the posture from this reference image, create a cutout transparent white-model posture PNG, and save a bilingual prompt Markdown file in the same directory."

policy:
  allow_implicit_invocation: true
