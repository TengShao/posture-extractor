interface:
  display_name: "Posture Generator"
  short_description: "Generate clean white-model pose PNGs"
  default_prompt: "Use $posture-generator to create a cutout transparent white-model pose PNG from this reference image."

policy:
  allow_implicit_invocation: true
